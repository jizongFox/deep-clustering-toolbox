Arch:
  name: preresnet
  num_classes: 10
  depth: 110

Optim:
  name: Adam
  lr: 0.0001

Scheduler:
  name: MultiStepLR
  milestones: [100,200,300,400,500,600,700,800,900]
  gamma: 0.7

DataLoader:
  batch_size: 100
  shuffle: true
  num_workers: 16
  drop_last: true

Trainer:
  max_epoch: 1000
  device: cuda
  save_dir: swa/baseline